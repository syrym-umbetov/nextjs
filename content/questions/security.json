[
  {
    "id": "sec-dal-requirements",
    "topic": "security",
    "difficulty": 2,
    "type": "multiple-choice",
    "prompt": "Каким трём требованиям должен отвечать Data Access Layer?",
    "options": [
      {
        "id": "a",
        "text": "Быть типизированным, покрытым тестами и вынесенным в отдельный пакет"
      },
      {
        "id": "b",
        "text": "Быть чистым, кэшируемым и не обращаться к сети"
      },
      {
        "id": "c",
        "text": "Выполняться в Route Handler, отдавать JSON и проверять CSRF"
      },
      {
        "id": "d",
        "text": "Выполняться только на сервере, проводить проверки авторизации и возвращать безопасные минимальные DTO"
      }
    ],
    "correctOptionId": "d",
    "explanation": "Документация формулирует требования к DAL списком из трёх пунктов: выполняться только на сервере, проводить проверки авторизации и возвращать безопасные минимальные Data Transfer Objects.\n\nСмысл в централизации: вся логика доступа к данным собрана в одном месте, поэтому проще обеспечить единообразие и меньше риск ошибок авторизации. Побочная выгода — общий in-memory кэш на запрос, который делят разные части приложения.",
    "distractors": [
      "Вариант про чистоту и отсутствие сети описывает совсем другой класс модулей: DAL как раз ходит в базу.",
      "Вариант с Route Handler переносит DAL на транспортный уровень — это внутренняя библиотека, а не эндпоинт.",
      "Вариант про типизацию и тесты перечисляет хорошие практики, но не то, что делает слой границей безопасности."
    ],
    "docsUrl": "https://nextjs.org/docs/app/guides/data-security#data-access-layer",
    "nextVersion": "16.3"
  },
  {
    "id": "sec-dto-whole-object",
    "topic": "security",
    "difficulty": 2,
    "type": "find-the-bug",
    "prompt": "Что опасного в передаче объекта пользователя из Server Component в Client Component?",
    "code": {
      "language": "tsx",
      "content": "export default async function Page() {\n  const user = await db.user.findUnique({ where: { id } })\n  return <ProfileCard user={user} />\n}"
    },
    "options": [
      {
        "id": "a",
        "text": "Пропсы сериализуются и целиком уезжают в браузер, включая поля, которые интерфейс не отображает, — вместо сырой записи надо отдавать минимальный DTO"
      },
      {
        "id": "b",
        "text": "Объект слишком велик и замедлит гидратацию — это вопрос производительности, а не безопасности"
      },
      {
        "id": "c",
        "text": "Опасности нет: Next.js вырезает из пропсов поля с чувствительными именами"
      },
      {
        "id": "d",
        "text": "Client Component не сможет прочитать вложенные поля, потому что сериализация плоская"
      }
    ],
    "correctOptionId": "a",
    "explanation": "Данные пересекают границу через пропсы, а пропсы сериализуются и отправляются в браузер. Передав сырую запись из базы, вы отдаёте пользователю **все** её поля — хэш пароля, внутренние флаги, чужие идентификаторы, — даже если интерфейс показывает только имя. Приём против этого — DTO: слой доступа возвращает безопасный минимальный объект, а не запись целиком.",
    "distractors": [
      "Вариант про автоматическое вырезание полей — опасная выдумка: фреймворк не угадывает, что чувствительно.",
      "Вариант про производительность подменяет проблему: замедление тут вторично, утечка первична.",
      "Вариант про плоскую сериализацию неверен: вложенные объекты сериализуются нормально."
    ],
    "footnote": "Дополнительный слой защиты — tainting: `experimental_taintObjectReference` для объектов и `experimental_taintUniqueValue` для конкретных значений, включаются опцией `experimental.taint`. Но документация подчёркивает, что это именно дополнительный слой, а фильтровать данные всё равно надо в DAL.",
    "docsUrl": "https://nextjs.org/docs/app/guides/data-security#passing-data-from-server-to-client",
    "nextVersion": "16.3"
  },
  {
    "id": "sec-server-only",
    "topic": "security",
    "difficulty": 2,
    "type": "multiple-choice",
    "prompt": "Что делает пакет `server-only` и как он себя проявляет?",
    "options": [
      {
        "id": "a",
        "text": "Помечает модуль как серверный: если он попадёт в клиентское окружение, сборка упадёт с ошибкой"
      },
      {
        "id": "b",
        "text": "Вырезает модуль из клиентского бандла молча, оставляя вместо него заглушку"
      },
      {
        "id": "c",
        "text": "Помечает функции как Server Actions — это альтернатива директиве `'use server'`"
      },
      {
        "id": "d",
        "text": "Шифрует содержимое модуля перед отправкой клиенту"
      }
    ],
    "correctOptionId": "a",
    "explanation": "Импорт `'server-only'` в начале модуля гарантирует, что проприетарный код и внутренняя бизнес-логика останутся на сервере: попытка импортировать такой модуль в клиентском окружении вызывает **ошибку сборки**. Ошибка вместо тихого поведения — в этом весь смысл: проблему видно до деплоя.\n\nДеталь про установку: Next.js обрабатывает импорты `server-only` внутренне, содержимое пакета из NPM не используется. Но если правила линтера ругаются на лишние зависимости, пакет можно установить.",
    "distractors": [
      "Вариант с молчаливой заглушкой описывает противоположное поведение — молчание здесь было бы опасно.",
      "Вариант с шифрованием путает с шифрованием переменных, захваченных замыканием Server Action.",
      "Вариант про альтернативу `'use server'` смешивает два разных механизма."
    ],
    "docsUrl": "https://nextjs.org/docs/app/guides/data-security#preventing-client-side-execution-of-server-only-code",
    "nextVersion": "16.3"
  },
  {
    "id": "sec-next-public",
    "topic": "security",
    "difficulty": 1,
    "type": "find-the-bug",
    "prompt": "Почему этот код — утечка секрета?",
    "code": {
      "language": "ts",
      "content": "// .env\n// NEXT_PUBLIC_STRIPE_SECRET_KEY=sk_live_...\n\nexport default async function Page() {\n  const charges = await stripe(process.env.NEXT_PUBLIC_STRIPE_SECRET_KEY)\n  return <ChargeList charges={charges} />\n}"
    },
    "options": [
      {
        "id": "a",
        "text": "Утечки нет: Server Component выполняется на сервере, значение в браузер не попадёт"
      },
      {
        "id": "b",
        "text": "Префикс `NEXT_PUBLIC_` явным образом открывает переменную клиенту: её значение встраивается в бандл и видно любому посетителю"
      },
      {
        "id": "c",
        "text": "Переменные окружения в Server Component читать нельзя, нужен Route Handler"
      },
      {
        "id": "d",
        "text": "Проблема только в имени: достаточно переименовать переменную, не меняя префикс"
      }
    ],
    "correctOptionId": "b",
    "explanation": "По умолчанию переменные окружения доступны только на сервере. Префикс `NEXT_PUBLIC_` — это явное указание открыть переменную клиенту: её значение подставляется в клиентский бандл на сборке и доступно любому, кто откроет исходники страницы.\n\nДля секретов префикс не используют никогда, как бы ни было удобно. Читать секрет надо в серверном коде из переменной **без** префикса.",
    "distractors": [
      "Вариант «утечки нет, потому что Server Component» упускает главное: дело не в том, где переменная читается, а в том, что префикс встраивает её значение в клиентский бандл.",
      "Вариант с запретом на чтение переменных в Server Component неверен — это как раз правильное место.",
      "Вариант про переименование без смены префикса ничего не меняет: решает именно префикс."
    ],
    "docsUrl": "https://nextjs.org/docs/app/guides/data-security#tainting",
    "nextVersion": "16.3"
  },
  {
    "id": "sec-proxy-optimistic",
    "topic": "security",
    "difficulty": 3,
    "type": "find-the-bug",
    "prompt": "Проверка авторизации вынесена в `proxy.ts` и ходит в базу. Что здесь не так?",
    "code": {
      "language": "ts",
      "content": "// proxy.ts\nimport { NextResponse, type NextRequest } from 'next/server'\n\nexport default async function proxy(req: NextRequest) {\n  const sessionId = req.cookies.get('session')?.value\n  const session = await db.session.findUnique({ where: { id: sessionId } })\n  if (!session) return NextResponse.redirect(new URL('/login', req.url))\n  return NextResponse.next()\n}"
    },
    "options": [
      {
        "id": "a",
        "text": "Нужно перенести проверку в корневой layout — это рекомендуемое место"
      },
      {
        "id": "b",
        "text": "В proxy вообще нельзя читать cookie — только заголовки"
      },
      {
        "id": "c",
        "text": "Proxy выполняется на каждом роуте, включая префетченные, поэтому из него читают только сессию из cookie — оптимистичная проверка; обращения к базе бьют по производительности"
      },
      {
        "id": "d",
        "text": "Проверка в proxy достаточна, проблема лишь в том, что нет обработки ошибок базы"
      }
    ],
    "correctOptionId": "c",
    "explanation": "Проблемы две, и обе важные.\n\nПервая — производительность. Proxy выполняется на **каждом** роуте, включая префетченные. Поэтому документация советует читать оттуда только сессию из cookie — это и есть оптимистичная проверка — и избегать обращений к базе.\n\nВторая — глубина защиты. Proxy полезен для первичных проверок, но не должен быть единственной линией обороны. Большинство проверок надо проводить как можно ближе к источнику данных, то есть в Data Access Layer.",
    "distractors": [
      "Вариант «проверки в proxy достаточно» повторяет ровно то заблуждение, от которого документация предостерегает.",
      "Вариант с запретом читать cookie неверен: чтение cookie — как раз рекомендуемый способ.",
      "Вариант с корневым layout предлагает не лучшее место: проверка в layout тоже не является границей, потому что запросы к данным можно слать в обход интерфейса."
    ],
    "docsUrl": "https://nextjs.org/docs/app/guides/authentication#optimistic-checks-with-proxy-optional",
    "nextVersion": "16.3"
  },
  {
    "id": "sec-return-values",
    "topic": "security",
    "difficulty": 3,
    "type": "multiple-choice",
    "prompt": "Что документация советует делать с возвращаемыми значениями Server Action?",
    "options": [
      {
        "id": "a",
        "text": "Всегда возвращать полную запись, чтобы клиент мог обновить кэш без дополнительного запроса"
      },
      {
        "id": "b",
        "text": "Возвращать только строки — объекты через границу действия не сериализуются"
      },
      {
        "id": "c",
        "text": "Не возвращать ничего: действия должны только мутировать данные"
      },
      {
        "id": "d",
        "text": "Ограничивать их: возвращаемое сериализуется и уходит клиенту, поэтому форму значения подгоняют под то, что рендерит интерфейс, а не отдают сырые записи базы"
      }
    ],
    "correctOptionId": "d",
    "explanation": "Требование стоит в одном ряду с аутентификацией и валидацией входных данных: возвращаемые значения действия сериализуются и отправляются клиенту, поэтому их форму нужно **ограничивать** тем, что рендерит интерфейс, а не отдавать сырые записи из базы. Это та же логика, что и с DTO при передаче данных в Client Component, только на обратном пути.",
    "distractors": [
      "Вариант «всегда возвращать полную запись» описывает ровно ту ошибку, от которой предостерегают: удобство обновления кэша не окупает утечку полей.",
      "Вариант «не возвращать ничего» слишком строг: действия штатно возвращают значения, их читает `useActionState`.",
      "Вариант «только строки» неверен: объекты прекрасно сериализуются."
    ],
    "footnote": "Отдельно для разрушительных операций вроде удаления документация советует усиленную обработку — проверку повышенного уровня сессии или переаутентификацию, с громким отказом, если проверка не прошла.",
    "docsUrl": "https://nextjs.org/docs/app/guides/server-actions#security",
    "nextVersion": "16.3"
  }
]
